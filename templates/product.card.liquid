{% if product == nil %}
  {% assign product = all_products[request.path | split: '/' | last] %}
{% endif %}

{% comment %}
  Template for AJAX/fetch view to return product card only
{% endcomment %}

{% assign product = product %}
{% render 'product-card-recently', product: product %}