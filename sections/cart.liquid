{% comment %}
  This section is used in the cart template to render /cart page with an
  overview of the items in customer's cart.

  https://shopify.dev/docs/storefronts/themes/architecture/templates/cart
{% endcomment %}
{% comment %} <h1>{{ 'cart.title' | t }}</h1> {% endcomment %}

<form action="{{ routes.cart_url }}" method="post">
  <table>
    {% for item in cart.items %}
      <tr>
        <td>
          {% render 'image', image: item.image, url: item.url %}
        </td>
        <td>
          <p>{{ item.product.title }}</p>
          {% comment %} {{ 'cart.remove' | t | link_to: item.url_to_remove }} {% endcomment %}
        </td>
        <td>
          <input type="text" name="updates[]" value="{{ item.quantity }}">
          {% comment %} <input type="submit" value="{{ 'cart.update' | t }}"> {% endcomment %}
        </td>
      </tr>
    {% endfor %}
  </table>

  {% comment %} <input type="submit" name="checkout" value="{{ 'cart.checkout' | t }}"> {% endcomment %}
</form>

{% schema %}
{
  "name": "t:general.cart",
  "settings": []
}
{% endschema %}
